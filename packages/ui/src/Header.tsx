import Head from "next/head";

export const Header = () => {
  return (
    <Head>
      <title>Pencil Explorer</title>
      <meta name="viewport" content="initial-scale=1.0, width=device-width" />
    </Head>
  );
};
